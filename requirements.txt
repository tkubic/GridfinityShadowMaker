opencv_python
Pillow
ezdxf
pyperclip
numpy
colorama
fonttools
iniconfig
packaging
pytest
typing_extensions
pyqt5

# Notes:
# - Versions are intentionally left unpinned to allow pip to select
#   compatible prebuilt binary wheels for the user's Python version.
# - If you require reproducible installs for CI, create a separate
#   `requirements-lock.txt` with pinned versions and use that in CI.